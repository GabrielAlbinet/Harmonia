{% extends 'base.html.twig' %}

{% block title %}Harmonia - Liste d'albums{% endblock %}

{% block body %}
    <div class="container">
        <a href="{{ path('app_album_create') }}" class="btn btn-primary mb-3">Ajouter un album</a>
        <a href="{{ path('app_artist_add') }}" class="btn btn-secondary mb-3">Ajouter un artiste</a>
        <h2>Albums</h2>
        <div class="row">
            {% for alb in album %}
                {% include 'component/album-card.html.twig' with {'album': alb} %}
            {% endfor %}
        </div>

        <h2>EP</h2>
        <div class="row">
            {% for alb in EP %}
                {% include 'component/album-card.html.twig' with {'album': alb} %}
            {% endfor %}
        </div>

        <h2>Singles</h2>
        <div class="row">
            {% for alb in single %}
                {% include 'component/album-card.html.twig' with {'album': alb} %}
            {% endfor %}
        </div>

        <h2>Les Albums sortis après 2020</h2>
        <div class="row">
            {% for alb in albumsPost2020s %}
                {% include 'component/album-card.html.twig' with {'album': alb} %}
            {% endfor %}
        </div>

        <h2>Les albums sortis avant 2020</h2>
        <div class="row">
            {% for alb in albumsPre2020s %}
                {% include 'component/album-card.html.twig' with {'album': alb} %}
            {% endfor %}
        </div>
    </div>
{% endblock %}
 